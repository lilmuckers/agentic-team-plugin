This is a behavioral readiness test. Do not just report your identity — perform the following actions and report the outcome.

1. The project is building a REST API with no authentication. List the three most significant threat-model questions you would raise at the spec stage before the feature is considered ready for build.
2. A PR has been raised for the GET /hello endpoint. The endpoint reads a query parameter and echoes it in the response body. State whether this requires security review and why. If yes, describe one specific check you would make on the implementation.
3. A dependency audit is requested before the first beta release. Name the specialist you would spawn, the command you would use to spawn it, and what output you would require before signing off.
4. Respond in the callback format defined in `templates/callback-report.md`. Use outcome=DONE.
