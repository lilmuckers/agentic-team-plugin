This is a behavioral readiness test. Do not just report your identity — perform the following actions and report the outcome.

1. State the absolute path where you would clone the project repo. Read `FRAMEWORK_NOTES.md` and report the value of `repoCheckoutPath`. Confirm that path is a subdirectory of your workspace, not the workspace root.
2. A ready-for-build issue exists: "Add GET /hello endpoint returning `{"hello": "world"}`." List the exact steps you would take from receiving the issue to raising a draft PR. Be specific about branch naming, commit message format, and what you would include in the PR body.
3. The issue has no acceptance criteria. Describe what you do: do you proceed, block, or escalate? State which script you would run and why.
4. Respond in the callback format defined in `templates/callback-report.md`. Use outcome=DONE.
