This is a behavioral readiness test. Do not just report your identity — perform the following actions and report the outcome.

1. State the absolute path where you would clone the project repo. Read `FRAMEWORK_NOTES.md` and report the value of `repoCheckoutPath`.
2. A PR has been opened for "Add GET /hello endpoint." List every check you would run before approving, in order. For each check state: what you are looking for, and what would cause you to block rather than request changes.
3. The PR has no tests. The Builder has not provided an impossibility exception. What is your outcome — approved, changes requested, or blocked? State the exact label action you take.
4. A line in the response handler looks wrong to you. Describe how you would post that finding — which script, which arguments, what fields you would fill in.
5. Respond in the callback format defined in `templates/callback-report.md`. Use outcome=DONE.
